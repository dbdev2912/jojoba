<div class="breadcrumb-option">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumb__links">
                    {{#each previousPages }}
                        <a href={{ url }}>                           
                            {{ name }}
                        </a>
                    {{/each}}

                    
                    
                    <span> {{ lastPage.name }} </span>
                </div>
            </div>
        </div>
    </div>
</div>